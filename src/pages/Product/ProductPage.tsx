import { HeaderTitle } from '@/components/common';

export default function ProductPage() {
    return (
        <>
            <HeaderTitle title="상품" onSubmit={() => {}} />
            <section>아직 등록된 상품이 없습니다.</section>
        </>
    );
}
